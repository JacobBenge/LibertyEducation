<% layout('layouts/default-boilerplate') %>
<!--DEFAULT ERROR PAGE THAT IS RESPONDS WITH ERROR MESSAGE, STATUSCODE, AND ERROR STACK.-->
<div class="row">
    <div class="col-8 offset-2">
        <div class="alert alert-danger" role="alert">
            <h4 class="alert-heading"><%=err.message%></h4>
            <!-- ALTERNATIVELY WE CAN DELETE THE ERROR STACK FROM THE PRODUCTION VERSION -->
            <% if(process.env.NODE_ENV !== "production") { %> 
                <p><%= err.stack %></p>
            <% } %> 
        </div>
    </div>
</div>
